// src/components/organization/invite/constants.ts

/**
 * Fallback images for the invite page's Africa map hero, used when the
 * organization has no banner image configured.
 */
export const INVITE_FALLBACK_MAP_IMAGES = [
    "/landing/g.webp",
    "/landing/b.webp",
    "/landing/h.webp",
];
